/*
*Variables, Data Types, and Typing
 */
let wordData;

wordData = "Word list";

console.log(wordData);

let subTotal = 100;
let tax = subTotal * .07;
let totalWithTax = tax + subTotal;

/* Data Types
*
* Boolean: 2 values, True or False
* No: Value assigned is nothing
* Undefined: Set by js, similar to above
* Number = All numbers for which math can be performed
* Strings = Alphanumeric characters and codes */

let obj = {
    key1: "value",
    key2: 3,
    obj2: {
        obj2key1: "value of inner object",;
}
boolean: true,
    obj2: {
    obj2Key1: "Internal Object Value",;
}
};

console.log(obj.obj2.obj2Key1);

let variable = "string";

let var1 = "2";
let var2 = 5;
let result = var1 + var2;

console.log(result);

/* Operators are to perform mathematical operations on our values */

let add = 1 + 2;
let sub = 2 - 1;
let muly = 2 * 4;
let division = 4 / 2;
let mod = 5 % 2;

let string = "String 1 " + "String 2";

console.log(string);